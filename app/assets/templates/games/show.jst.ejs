<div id="streams">
  <h3><%= game.get("name") %> Streams</h3>
  <% game.streams().each(function (stream) { %>
    <div class="stream">
      <a href="#/streams/<%= stream.get("friendly_id") %>"
         data-toggle="tooltip" title="<%= stream.get("status") %>">
        <img src="<%= stream.get("preview") %>" alt="<%= stream.get("name") %>-box">
      </a>
      <div class="stream-text">
        <a href="#/streams/<%= stream.get("id") %>">
          <%= stream.get("name") %>
        </a>
        Viewers: <%= stream.get("viewers") %>
      </div>
    </div>
  <% }) %>
</div>

<script>
  $("#streams").gridalicious({
    selector: '.stream',
    gutter: 10,
    width: 250,
    animate: true,
    animationOptions: {
      speed: 100
    }
  });
  
  $('[data-toggle="tooltip"]').tooltip();
</script>

